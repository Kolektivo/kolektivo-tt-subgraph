{
  "name": "kolektivo-community-events",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ kolektivo-community-events",
    "create-local": "graph create --node http://localhost:8020/ kolektivo-community-events",
    "remove-local": "graph remove --node http://localhost:8020/ kolektivo-community-events",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kolektivo-community-events",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.73.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
